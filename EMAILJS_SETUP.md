# دليل إعداد EmailJS لصفحة تقييم العملاء

## نظرة عامة

تم إنشاء صفحة تقييم العملاء بنجاح! لكي تعمل الصفحة وترسل التقييمات إلى بريدك الإلكتروني، تحتاج إلى إعداد حساب EmailJS المجاني.

## خطوات الإعداد

### 1. إنشاء حساب EmailJS

1. اذهب إلى [https://www.emailjs.com/](https://www.emailjs.com/)
2. قم بإنشاء حساب مجاني (Sign Up)
3. سجل الدخول إلى لوحة التحكم

### 2. إنشاء Email Service

1. من لوحة التحكم، اذهب إلى "Email Services"
2. اضغط على "Add New Service"
3. اختر مزود البريد الإلكتروني (Gmail مثلاً)
4. اتبع التعليمات لربط حسابك
5. احفظ **Service ID** (ستحتاجه لاحقاً)

### 3. إنشاء Email Template

1. اذهب إلى "Email Templates"
2. اضغط على "Create New Template"
3. استخدم القالب التالي:

```
Subject: ⭐ تقييم جديد من عميل - الإيمان للزيتون

From: {{customer_name}}
Email: {{customer_email}}
Phone: {{customer_phone}}

التقييم: {{stars}} ({{rating}}/5) - {{rating_text}}

التعليق:
{{review_text}}

التاريخ: {{date}}

---
تم الإرسال من صفحة تقييم العملاء
الإيمان للزيتون 🫒
```

4. احفظ القالب واحفظ **Template ID**

### 4. الحصول على Public Key

1. اذهب إلى "Account" > "General"
2. انسخ **Public Key** (أو User ID)

### 5. تحديث ملف customer-review.js

افتح ملف `customer-review.js` وقم بتحديث الأسطر التالية:

```javascript
// السطر 3
emailjs.init("YOUR_PUBLIC_KEY"); // ضع Public Key هنا

// السطر 150
const response = await emailjs.send(
    'YOUR_SERVICE_ID',     // ضع Service ID هنا
    'YOUR_TEMPLATE_ID',    // ضع Template ID هنا
    {
        to_email: 'samymno23@gmail.com', // عنوان بريدك الإلكتروني
        // ... بقية البيانات
    }
);
```

## مثال على التحديث:

```javascript
// قبل التحديث
emailjs.init("YOUR_PUBLIC_KEY");

// بعد التحديث (مثال)
emailjs.init("user_abc123xyz789");

// قبل التحديث
const response = await emailjs.send(
    'YOUR_SERVICE_ID',
    'YOUR_TEMPLATE_ID',
    {...}
);

// بعد التحديث (مثال)
const response = await emailjs.send(
    'service_gmail123',
    'template_review456',
    {...}
);
```

## اختبار النظام

1. احفظ جميع التعديلات
2. افتح صفحة `customer-review.html` في المتصفح
3. املأ النموذج واختر تقييماً
4. اضغط على "إرسال التقييم"
5. تحقق من بريدك الإلكتروني

## ملاحظات مهمة

- ✅ الحساب المجاني يسمح بـ 200 رسالة شهرياً
- ✅ يمكنك استخدام أي مزود بريد إلكتروني (Gmail, Outlook, Yahoo, إلخ)
- ✅ جميع التقييمات ستصل مباشرة إلى بريدك الإلكتروني
- ✅ نظام النجوم يعمل بشكل تفاعلي وجميل
- ⚠️ لا تشارك Public Key أو Service ID مع أي شخص

## الدعم الفني

إذا واجهت أي مشاكل:
- تحقق من console في المتصفح (F12)
- راجع توثيق EmailJS: [https://www.emailjs.com/docs/](https://www.emailjs.com/docs/)
- تأكد من صحة جميع المعرفات (IDs)

## ميزات الصفحة

✨ نظام تقييم بالنجوم تفاعلي وجميل
✨ تحقق من صحة البيانات في الوقت الفعلي
✨ رسائل خطأ واضحة بالعربية
✨ تصميم متجاوب يعمل على جميع الأجهزة
✨ إرسال مباشر للبريد الإلكتروني (بدون واتساب)
✨ رسائل نجاح مميزة
✨ تنسيق احترافي وجذاب

---

🫒 **الإيمان للزيتون** - نظام تقييم العملاء الاحترافي
